# Copy to .env and fill in, or just export these in your shell.
#
# The app has no external dependencies. The result copy is a lookup table in
# public/result-copy.js, resolved in the browser, so nothing here is required to
# run it end to end.

# Optional. Default 3000.
# PORT=3000
